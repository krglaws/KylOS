
#include "include/proc.h"


unsigned int kill_proc(const unsigned int pid)
{
	// check if proc exists in process tree
		// return if not

	// check if killer has permission
		// return if not

	// everything looks good
	
	// remove from scheduler queue
	
	// remove from allocation table
	
	// remove from process tree
	
	return 0;
}

